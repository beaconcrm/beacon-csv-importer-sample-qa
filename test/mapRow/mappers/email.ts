import assert from 'assert';
// This is the function we are testing
// import emailMapper from '../../../src/mapRow/mappers/email';

describe('#emailMapper', () => {

  it('should map', () => {
    assert.deepStrictEqual(true, true);
  });
});
